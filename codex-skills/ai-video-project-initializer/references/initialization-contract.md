# 项目初始化契约

## 交付边界

| 系统 | 负责人初始化的内容 | 长期事实源 |
| --- | --- | --- |
| Git | 规则、剧本、分镜、提示词、清单、岗位技能、模板与版本记录 | 文本与规则 |
| 飞书 | 任务、审核、问题、变更、每日推进和成员岗位 | 动态状态 |
| 个人百度网盘 | 每位成员的 API 交付中转目录 | 待审核媒体 |
| 百度网盘团队空间 | 审核后公共资产、可剪辑素材、工程、审片版、成片 | 正式大媒体 |

## Git 最小结构

```text
AGENTS.md
README.md
.gitignore
00_项目中枢/
  当前版本索引.md
  项目总路线图.md
  组织与岗位配置-V1.0.md
  飞书中枢配置-V1.0.md
  百度网盘交接目录-V1.0.md
roles/
skills/
  project-onboarding/
  netdisk/
  writer/
  storyboard/
  asset/
  video/
  editor/
templates/
  task-card.md
  issue.md
  change-request.md
  新成员接入包模板.md
剧本/
提示词/
资产清单/
剪辑清单/
制作/
角色资产/
scripts/
  feishu/
  onboarding/
  baidu-netdisk/
```

各目录可按项目补充；不得将图片、音频、视频、剪辑工程、令牌、成员私有接入包纳入版本控制。

## 技能源与项目拷贝

初始化者从 `https://github.com/199184796qq-crypto/projectskills` 的 `codex-skills/` 拉取 skill；不扫描本机既有 skill 作为项目能力来源。按启用岗位和指定视频工具挑选完整 skill 目录及其直接引用资源，复制到新项目的 `skills/`。

每个项目在 `skills/来源清单.md` 记录来源仓库、来源提交、选择日期、启用岗位／工具和每个已复制的 skill 目录。项目仓库只保存本项目实际需要的 skill；技能库缺失时记录缺口，不能用本机同名 skill 静默替代。

## Git 授权与提交门禁

- Git 授权只针对**当前项目仓库**：成员用自己的 GitHub／Git 账号克隆、拉取和推送，不共享负责人账号、访问令牌或 SSH 私钥，也不获得其他仓库权限。
- 项目负责人保留仓库管理和受保护 `main` 的合并权限；制作成员获得当前仓库所需的读取与任务分支写入权限；仅查看者只读。需要更细的 Read／Write／Maintain／Admin 分级时，优先将项目放在 GitHub Organization 中管理。
- 成员领取任务后，在 `member/<成员标识>/<任务编号>` 分支提交本岗位文本；提交信息带岗位、范围与版本。完成后推送该分支并创建合并请求，负责人或指定审核人通过后合并 `main`。
- 对 `main` 启用仓库规则或分支保护：要求合并请求审核、阻止直接推送、阻止强制推送与删除。规则应允许负责人在紧急情况下按项目约定处理，而不是让所有成员拥有绕过权限。
- 可推送的只有规则、剧本、分镜、提示词、清单、审核记录和轻量文档。图片、音频、视频、剪辑工程和凭据由 `.gitignore` 排除；发现误提交立即停止后续交付并由负责人处理，不以覆盖提交掩盖敏感内容。

## 飞书初始化清单

建立以下六张关联表：

1. **项目／集数**：项目、集数、阶段、总负责人、当前版本。
2. **任务**：任务编号、范围、岗位、负责人、审核人、归档管理员、输入 Git 文件与版本、媒体输入、个人盘交付目录、个人盘输出链接、团队归档目标／链接、归档状态、Git 输出提交、状态、问题／变更、修改摘要。
3. **资产**：资产编号、类型、版本、用途、来源、团队空间链接、审核状态、关联任务。
4. **问题与变更**：类型、依据版本、证据、影响范围、返工成本、方案、决策人、结论、关联任务。
5. **版本索引**：交付物、Git 路径、版本、状态、批准人、关联任务。
6. **成员与岗位**：成员、允许岗位（多选）、当前状态、飞书身份、交付根目录。

任务状态固定为：`待排期 → 待输入 → 可执行 → 制作中 → 待审核 → 已确认`；例外状态为 `阻塞`、`需复核`、`已失效`。为每个启用岗位建立“我的可执行任务”视图：岗位匹配、负责人为该成员或未分配、状态为“可执行”。

## 网盘目录与流转

成员个人盘中的任务目录采用：

```text
/《项目名》_个人交付中转/<成员>/<任务编号>/
```

团队空间中的正式归档目录采用：

```text
《项目名》/
  02_角色场景道具资产/
  03_音频与配音/
  05_视频候选/
  06_已审核可剪辑素材/
  07_剪辑工程与代理文件/
  08_审片版/
  09_正式成片/
  99_历史归档/
```

成员的官方 MCP 仅上传个人盘绝对路径。审核通过后，归档管理员导入团队空间、比对文件名／大小／版本／用途、填写团队空间链接，再将任务确认。候选视频在审核确认前不得进入 `06_已审核可剪辑素材/`。

## 个人岗位接入 Markdown

实际文件存放在项目仓库外的受控目录，或在仓库的已忽略目录中；绝不包含飞书 App Secret、管理员令牌或其他成员的个人凭据。

项目仓库还必须提供一份受版本控制的 `skills/岗位技能安装映射.md`：每个岗位列出项目内职责／wrapper、公共 `projectskills` 的完整目录路径、适用工具和安装优先级。成员选择多个岗位时，取各岗位清单的并集；同一公共 skill 只安装一次。项目 wrapper 不是可脱离项目的通用 skill，成员应从克隆仓库读取，不复制进全局 skills 目录。

成员的 Codex 按下列顺序完成本地能力接入：

1. 克隆项目仓库，读取 `README.md`、`AGENTS.md`、岗位职责、`skills/岗位技能安装映射.md` 与项目接入 skill。
2. 根据允许岗位（可多选）计算所需公共 skill 的并集，只从 `https://github.com/199184796qq-crypto/projectskills` 的映射目录下载完整 skill 文件夹；不得因名称相近而替换为本机其他 skill。
3. 安装到该成员自己的 Codex 本地 skills 目录；先检查同名目录。来源仓库、来源提交和内容一致时可复用，否则以 `<项目标识>-<skill>` 的命名空间安装或向项目负责人报告冲突，禁止覆盖成员原有 skill。
4. 逐一读取已安装的 `SKILL.md`，在被 Git 忽略的本地连接记录中写入安装日期、来源提交、目录和结果。仅当项目 wrapper 与所需公共 skill 都已读取，才可领取“可执行”任务。

```markdown
# 《项目名》｜<成员姓名> 岗位接入

- 项目 Git 地址：<Git URL>
- 飞书显示名：<成员姓名>
- 允许岗位：<岗位 A>、<岗位 B>
- 个人盘交付根目录：/<项目名>_个人交付中转/<成员姓名>/
- 团队空间权限：读取已归档资产／按任务下载

## 交给 Codex 的指令

加入 <项目名> 项目：<Git URL>。我的姓名是 <成员姓名>，允许岗位是 <岗位列表>。
飞书和个人网盘连接信息已通过接入包提供。请按 README 与
skills/project-onboarding/SKILL.md 初始化：先根据 `skills/岗位技能安装映射.md`
下载并安装我的全部岗位 skill 到本地，逐一读取确认；只领取本人且岗位匹配的“可执行”任务。
我承担多个岗位时，分别读取对应 roles 与 skills，但每张任务只按其责任岗位执行。
```

凭据另行通过受控渠道提供：个人受限飞书令牌、项目多维表格标识、该成员自己的百度网盘 Access Token。成员的 Codex 应将它们保存到被 Git 忽略的本地连接文件中。
